# SageMath worker (LLL / Coppersmith / ECC / finite fields).
# Heavy; the control plane limits this to one concurrent SAGE job (§54).
FROM sagemath/sagemath:latest

WORKDIR /workspace
ENTRYPOINT ["/home/sage/sage"]
